# 岁岁时光多端交互原型

直接在浏览器打开 `desktop/index.html` 即可体验，无需安装依赖或启动服务。

- `desktop/`：当前桌面端可点击原型。
- `mobile/`：后续移动端原型的独立入口预留。
- `shared/`：两端共享的模拟数据、设计变量和本地资源。

原型将演示数据保存到浏览器 `localStorage` 的 `sui-time-reference:v1` 键中；在设置菜单点击“恢复演示数据”，或清理此键，即可回到初始状态。
